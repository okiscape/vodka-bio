{
 "name": "okiscape-bio",
 "version": "1.0.0",
 "packageManager": "pnpm@9.0.0",
 "private": true,
 "workspaces": [
  "apps/*",
  "packages/*"
 ],
 "scripts": {
  "dev": "turbo run dev --filter=!.",
  "build": "turbo run build",
  "lint": "turbo run lint"
 },
 "devDependencies": {
  "turbo": "^2.0.0"
 }
}
